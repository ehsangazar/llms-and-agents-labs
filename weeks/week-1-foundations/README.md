# Week 1 · Foundations

Why demos die in production, and how to tell when a task actually needs an
agent. Two read-along sessions, then the week's lab.

| # | Folder | What it is |
|---|--------|-----------|
| S1 | [`s1-why-demos-die`](s1-why-demos-die) | The demo that dies under real traffic, then the five workflow patterns, runnable |
| S2 | [`s2-model-boundary`](s2-model-boundary) | The deterministic/model boundary: one task as a workflow vs as an agent, runnable |
| Lab 1 | [`lab-workflow-router`](lab-workflow-router) | Build a workflow router: classify, route to the cheapest capable tier, validate, fall back |

Start with S1, run S2 right after, then do the lab. The lab starts with
`npm test` and needs no API key; the session scripts do call a real model.
